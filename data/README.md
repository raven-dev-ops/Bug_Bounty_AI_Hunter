# Data

Local tracking data such as findings databases and program registries lives
here. Do not store sensitive data or real artifacts.

Key files:
- `findings_db.json` - Local findings database.
- `program_registry.json` - Generated program registry.
- `workflow_tracker.json` - Live bug bounty workflow progress tracker (used by `dashboard.html`).
