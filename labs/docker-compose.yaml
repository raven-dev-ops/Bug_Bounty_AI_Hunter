version: "3.8"
services:
  rag-app:
    image: python:3.11-alpine
    working_dir: /srv
    volumes:
      - ./stubs/rag-app:/srv:ro
    command: ["python", "-m", "http.server", "8000"]
    ports:
      - "8000:8000"
  vector-store:
    image: python:3.11-alpine
    working_dir: /srv
    volumes:
      - ./stubs/vector-store:/srv:ro
    command: ["python", "-m", "http.server", "8001"]
    ports:
      - "8001:8001"
