# Bugcrowd auth (do not commit real secrets).
#
# Recommended: cookie-based auth from an existing logged-in browser session.
# Paste the full Cookie header value for bugcrowd.com (not identity.bugcrowd.com).
BUGCROWD_COOKIE=

# Optional: username/password login via identity.bugcrowd.com.
# Note: May require OTP / backup codes (the script can prompt when run interactively).
BUGCROWD_EMAIL=
BUGCROWD_PASSWORD=
BUGCROWD_OTP_CODE=
BUGCROWD_BACKUP_OTP_CODE=

# Optional: file containing backup codes (one per line). If unset, the script will
# also look for `BUGCROWD_backup_codes` in the repo root.
BUGCROWD_BACKUP_CODES_FILE=

# Optional: remove a used backup code from BUGCROWD_BACKUP_CODES_FILE after a
# successful login. Values: 1/true/yes/on.
BUGCROWD_CONSUME_BACKUP_CODE=0

# Optional: allow the script to prompt for OTP/backup codes when required.
# Values: 1/true/yes/on. Prefer leaving this disabled for automated runs.
BUGCROWD_PROMPT_MFA=0
